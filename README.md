# Modern Closet — Landing Page (Front-end)

Este projeto é uma landing page de e-commerce fictício que eu desenvolvi para treinar **layout responsivo**, **componentização visual com Bootstrap** e **interações em JavaScript** (sem depender de framework). A ideia foi criar uma página moderna, leve e agradável de navegar — com detalhes que fazem diferença, como destaque de seções no menu, “carrinho” lateral e um botão flutuante de contato.

---

## ✨ Principais funcionalidades
- **Layout responsivo** (desktop, tablet e mobile)
- **Menu mobile** (abre/fecha e fecha ao clicar em um item)
- **Scroll Spy**: o menu marca a seção ativa conforme você navega
- **Tema por seção**: a página muda detalhes de cor conforme a seção em foco
- **Produtos em destaque** e **novidades** renderizados por JavaScript (arrays)
- **Cart Drawer (carrinho)** lateral com overlay e suporte ao **ESC** para fechar
- **Botão flutuante de contato (FAB)** com animação e formulário (demo)

---

## 🧰 Tecnologias usadas
- **HTML5**
- **CSS3**
- **JavaScript (Vanilla)**
- **Bootstrap 5** (CDN)
- **Bootstrap Icons** (CDN)
- **Google Fonts (Open Sans)**

> Não é um projeto Node/React — então não precisa de `npm install`. É só abrir o `index.html` (ou usar Live Server no VS Code).

---

## ▶️ Como rodar localmente
### Opção 1 — Abrindo direto
1. Baixe/clone o repositório
2. Abra o arquivo `index.html` no navegador

### Opção 2 — VS Code
1. Instale a extensão **Live Server**
2. Clique com o botão direito no `index.html`
3. Selecione **Open with Live Server**

---
## 📁 Estrutura do projeto
```text
/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
└── img/
    └── (imagens do layout)
